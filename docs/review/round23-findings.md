# Круг 23 — находки и адъюдикация (по волне 25, HEAD 263e751, дифф не закоммичен)

Два аудитора одним сообщением, непересекающиеся линзы:
* **A** — атака на новые механизмы волны (Н1-Н10 + ответы на семь авторских вопросов B1-B7).
* **B** — истинность 13 заявлений + четыре переписи (C1, C2, E1).

Адъюдикация контроллера. Помечено, что проверено ЛИЧНО и чем.

## Принято

| id | важность | суть | где | как проверено контроллером |
|---|---|---|---|---|
| R23-1 | высшая | `tools/costs-bench.py` и `tools/probes-sync-bench.sh` не зовёт НИКТО | вне себя costs-bench назван только в `docs/review/`; probes-sync-bench — только строкой таблицы OWNERS `claude-patch-all.sh:1202` (данные, не вызов) | лично: перепись ссылок; для контраста у живого corpus-tools-bench есть пусковой `tools/sweep.sh:694` |
| R23-2 | высокая | `exit($? >> 8)` превращает смерть ребёнка от сигнала в код 0 | `tools/sweep.sh:207` | лично ЗАМЕРОМ: `perl` + SIGKILL → сырой статус 9, `>>8` = 0 |
| R23-3 | высокая | слитый `trap … EXIT INT TERM` даёт код 0 на убитом прогоне — G-13 закрыт частично | `tools/backup-divergence-probe.sh:59`, `tools/checks-on-image.sh:75`, `scripts/kit-build.sh:52` | лично ЗАМЕРОМ: слитая форма → rc=0; только-EXIT → rc=143 |
| R23-4 | средняя | шапка кодов объявляет 0,1,2,5 — код возвращает 3 и 6 | `scripts/probes-sync.sh:10-15` против `:171,:179,:192,:208,:211,:219` | лично: чтение обеих сторон |
| R23-5 | средняя | каталог-замок на голом `kill -0` без метки старта — та же волна закрыла это в двух других замках | `scripts/probes-sync.sh:205,214` | принято по отчёту A; форма сверена с закрытым образцом `claude-patch-all.sh:317-339` |
| R23-6 | средняя | стадии `--from-home` текут в дерево РЕПО: обход идёт только по `PAIR_B` | `scripts/probes-sync.sh:144` (`dst="$A"`) против `:227-228` | лично: чтение `stage_one` и `for_each_sync_stage` |
| R23-7 | средняя | столкновение `mkdir` после перехвата протухшего замка отдаёт 6 («машинерия сломана») вместо 3 («держит другой») | `scripts/probes-sync.sh:208` | лично: чтение ветки |
| R23-8 | средняя | `--diff` красит ЖИВОГО писателя: у прополки проверка живости есть, у отчёта нет | `scripts/probes-sync.sh:243-246` против `:238` | лично: чтение обеих функций |
| R23-9 | средняя | запись lstart не проверяет непустоту: отказавший `ps` кладёт `PID\t`, и `--stop` отказывает ВЕСЬ прогон с неверным диагнозом | `tools/sweep.sh:360` | лично: чтение записи; проверки нет |
| R23-10 | низкая-средняя | «лидер мёртв, группа жива» диагностируется как «номер группы переиспользован» — заголовок неверен для этого случая | `tools/sweep.sh:90-93` | принято по отчёту A |
| R23-11 | низкая-средняя | фолбэк `context_window` может объявить окно БОЛЬШЕ ёмкости маршрута | `set-model-costs.py` ~`:169-202` | принято по отчёту A (скоуп волны A) |
| R23-12 | средняя | `costs-bench` с живыми 11/13 не внесён в `OWNERS` гейта чисел | `claude-patch-all.sh:1183-1209` | лично: чтение реестра — записи нет |
| R23-13 | низкая-средняя | инвентарь README не знает `costs-bench.py`, `probes-sync-bench.sh`, `scripts/probes-sync.sh` | `README.md:115-145` | лично: счёт вхождений = 0/0 |
| R23-14 | низкая | наследный мусор старой формы `.part.XXXXXX` не убирается НИКОГДА (прополка требует числовой суффикс) | `tools/fetch-corpus.sh:105-111` | принято по отчёту A |
| R23-15 | низкая | исключение переписи замков `:[0-9]*:  '` слишком широко: настоящее взятие замка, отформатированное как data-строка, выпадет молча | `tools/lock-probe.sh:309` | принято по отчёту A |
| R23-16 | низкая | `validated_nonnegative_integer` принимает 20-значное число, заворачивающееся в bash-арифметике в отрицательное | `claude-patch-all.sh` (`GATE_BUDGET`) | принято по отчёту A |
| R23-17 | документ | заявление «чужой дифф волны A не тронут» ложно: `scripts/probes-sync.sh` (+91) и строка README fd-7 — работа B, а не A. Причина пина 66 названа неполно: 4 из-за ЧЕТЫРЁХ пусковых, не одного `launch_sweep` | `docs/review/wave25-report-A.md`, будущий report-B | лично: подтверждено аудитором B, сверено с диффом |
| R23-18 | документ | у полосы B волны нет отчёта (`wave25-report-B.md` отсутствует) | `docs/review/` | лично: файла нет |

## Отклонено

**Нарратив в комментариях волны** (4 площадки с меткой «волна 25B»: `corpus-tools-bench.sh:2651-2655,1187-1188,1307-1308`, `judge-tools-bench.py:1407-1408`, `lock-probe.sh:297-299`).

Основание отклонения: правило «комментарий несёт только констрейнт» ограничено ПРОГРАММНЫМ КОДОМ (уточнение юзера 2026-08-25); в скриптах и патчах полные комментарии и история сохраняются. Кит — скрипты. Аудитор правила-исключения не знал; находка снята не по существу формы, а по области действия правила.

## Проверено и НЕ является дефектом (отрицательные вердикты с основанием)

* **Только-`EXIT` трап** (`tools/corpus-tools-bench.sh:196`, `claude-patch-all.sh:407`) — ЗАМЕР: код 143 и уборка исполняется. Правильное поведение; чинить нечего. Собирался «дочинить» — замер остановил.
* **Часовой `probes-sync.sh`** подменяет код только при `rc == 0`; отказ кодом 3 проходит насквозь, «оборвалась» не печатается.
* **Локаль lstart двери `--stop`** — `LC_ALL=C` на ОБЕИХ сторонах (`tools/sweep.sh:360` и `:89`).
* **`__stale_future` при двойном отказе `stat`** падает в закрытую сторону: неизмеримая метка = не удалять.
* **Держатель зонда** флочит приватный файл, не боевой замок; спавн с `9>&-`, свип зовёт с `8>&-`.
* **Невакуумность трёх новых стендов**: меряют боевые файлы, не копию логики.
