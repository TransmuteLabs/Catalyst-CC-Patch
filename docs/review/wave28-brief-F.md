# Волна 28, бриф F: классы кодов возврата и слепые пятна боевых гейтов

БАЗА: HEAD `5aa565f` + правки полосы E в рабочем дереве (пять стендов). Полоса E
УЖЕ ПРИМЕНЕНА — не переделывай её, работай поверх.

СКОУП ЗАПИСИ: `claude-patch-all.sh`, `tools/sweep.sh`, `tools/checks-teeth.py`,
`tools/corpus-tools-bench.sh`, `tools/build-path-probe.sh`, `tools/probe-bench.js`,
`tools/probes-sync-bench.sh`, `judge/replay.py`, `judge/adjudicate.py`,
`judge/validate.py`, `judge/compact.py`, `set-model-costs.py`, `claude_patch.py`,
`README.md`, `docs/review/`. Вне списка не трогать; замеченное — строкой в отчёт.

КОММИТОВ НЕ ДЕЛАТЬ. Комментарии в скриптах и патчах — полные, с историей и
причиной (правило «только констрейнт» здесь не действует).

ЕДИНАЯ ТАБЛИЦА КОДОВ КИТА: 0 зелено · 1 отказ по существу · 2 прибор не может
мерить · 3 замок держит другой живой прогон · 4 объявленное число не сошлось с
фактическим · 5 нечего мерить на этой машине · 6 окружение/машинерия сломаны.
Правило двустороннее: объявленный код обязан быть ДОСТИЖИМ, достижимый —
ОБЪЯВЛЕН, а вызывающий обязан ветвиться по КЛАССУ, не по «ноль/не ноль».

## F-1 (ВЫСШАЯ). Сломанная машинерия замка выдаётся за живую сборку

`tools/checks-teeth.py:146-155` — `hold_read_lock` при `open(..., "a")` OSError
(нет каталога, нет права) возвращает `None`; `:253-258` — ЛЮБОЙ `None` даёт код
3 и текст «замок конвейера держит живая сборка».

ПРОВЕРЕНО КОНТРОЛЛЕРОМ ЛИЧНО:

    CLAUDE_PATCH_LOCK=/tmp/нет-такого-каталога/x.lock python3 tools/checks-teeth.py --image README.md
    → rc=3
    checks-teeth: НЕ МЕРИЛИ -- замок конвейера держит живая сборка (...); повтор поможет

Каталога нет, держателя нет. Это регресс класса F-2 круга 18, ради которого
таблица кодов и заведена: «один код на два ответа — сломанная машинерия десять
минут выглядела как занятый замок». Свип на 3 уходит в «повторить позже»
(`sweep.sh:806-811`) и будет ждать несуществующего держателя.

СДЕЛАТЬ: `hold_read_lock` обязан РАЗЛИЧАТЬ «flock занят» и «замок не открыть».
Занят → 3. Не открыть (OSError на open, неработающий flock) → 6 «машинерия
сломана, повтор не поможет», с текстом, называющим настоящую причину. Оба кода
уже объявлены в шапке файла — новых заводить не нужно.

## F-2 (высокая). Главный конвейер остался на слитой форме трапа

`claude-patch-all.sh:407` — единственный трап: `trap '__release_lock' EXIT`.
Нет `trap 'exit 130' INT` и `trap 'exit 143' TERM`.

ИЗМЕРЕНО НА ЭТОЙ МАШИНЕ (факт, не гипотеза): при `trap … EXIT` в одиночку TERM
приходит в часового как `__rc=0`, часовой НАЗЫВАЕТ остановку «ошибкой оболочки»
(подстановка/`set -u`) и выходит 1, а `wait` родителя всё равно даёт 143. То
есть на stderr ложный диагноз. Волна 26 расщепила трапы у `sweep.sh`,
`fetch-corpus.sh`, `kit-build.sh`, `probes-sync.sh`, `build-path-probe.sh`,
`checks-on-image.sh`, `backup-divergence-probe.sh`, `lock-probe.sh` — главный
35-минутный скрипт пропущен.

СДЕЛАТЬ: расщепить так же — `trap '__release_lock' EXIT`, `trap 'exit 130' INT`,
`trap 'exit 143' TERM`. Убедиться, что освобождение замка по-прежнему происходит
(EXIT-трап срабатывает и после `exit 143`).

## F-3 (высокая). Корпусный стенд — та же слитая форма

`tools/corpus-tools-bench.sh:199` — `trap cleanup EXIT` без INT/TERM. Сценарий
91 S4 требует 143 БЕЗ строки «ошибка оболочки» у свипа, наполнителя и зонда
пути — у самого стенда этой проверки нет. Расщепить так же.

## F-4 (высокая). Дверь self-check стенда зондов роняет классы 2 и 4

`claude-patch-all.sh:5438-5445`: `case` печатает верный текст для 2 и 4, а затем
`exit 1` для всех веток. Первый прогон того же стенда (`:5368-5430`) классы
СОХРАНЯЕТ (`exit "$__benchrc"`), и соседи тоже: judge-tools `:1138-1142`,
costs `:1176-1180`, probes-sync-bench `:1199-1203`, docnum `:2039-2044`.

СДЕЛАТЬ: сохранять класс — `exit "$__rc"` для 2 и 4, прочее остаётся 1.

## F-5 (средняя). У свипа нет руки 6 на зубах реестра

`tools/sweep.sh:801-823`. Ребёнок `checks-teeth.py` объявляет и возвращает 6
(`:240-242`, нет `tools/checks-on-image.sh`). Свип проверяет наличие
`checks-teeth.py` (`:795-797`), а РАННЕР не проверяет; неполный снимок кита даёт
6, и `*)` печатает «проверка прошла мутацию молча либо покраснела чужой дверью»
— чужой класс, чужой текст. Рядом у `corpus-tools-bench` рука 6 ЕСТЬ
(`:765-768`) — сделать по образцу.

## F-6 (средняя). У свипа нет руки 4 на корпусном стенде + честная формулировка

`tools/sweep.sh:758-777`: у корпусного стенда объявлен и достижим код 4
(`--table-check`, `check_mut_tables`), но он падает в `*)` «стенд не сошёлся».
Завести руку 4 по образцу соседа (`:816-819`).

СВЯЗАННОЕ РЕШЕНИЕ КОНТРОЛЛЕРА (закрывает adjudication request линзы A №1).
Конвейер сегодня схлопывает 4 стендов в `exit 1`, потому что рука 4 свипа для
КОНВЕЙЕРА несёт конкретное утверждение — `sweep.sh:1039`: «конвейер вернул 4:
цель не те байты, что названы пином». Это не ложь, но класс теряется. Полный
вариант, обе части сразу:
  (а) расширить текст руки 4 в `sweep.sh:1039` до честного общего: «конвейер
      вернул 4: объявленное число не сошлось с фактическим — байты цели против
      пина либо длина/покрытие таблицы мутаций стенда»;
  (б) в `claude-patch-all.sh` вернуть код 4 там, где сегодня класс 4 ребёнка
      схлопывается в 1: `:1143-1144`, `:1181-1182`, `:1204-1205`, `:2045-2046`
      и дверь F-4. Текст сообщений не менять — он уже верный.

## F-7 (средняя). Зонд пути не ветвит код 6 пробы стража

`tools/build-path-probe.sh:252-260`: `backup-divergence-probe.sh` объявляет 6
(нет python3), вызывающий отделяет только 2, а 6 читается как 1 «таблица стража
не сошлась». Добавить руку 6 по образцу соседней ветки для lock-probe.

## F-8 (низкая). 130/143 достижимы и ни в одной шапке не объявлены

Волна 26 СПЕЦИАЛЬНО завела `trap 'exit 130' INT` / `trap 'exit 143' TERM`, плюс
`sweep.sh:237` поднимает смерть ребёнка форк-запаски в `128 + ($? & 127)`.
Шапки обещают только подмножество 0–6.

РЕШЕНИЕ КОНТРОЛЛЕРА (закрывает adjudication request линзы A №3): смерть по
сигналу — POSIX, а не вердикт кита, поэтому в таблицу классов она НЕ вводится;
но двустороннее правило требует объявления. В шапку КАЖДОГО файла со
split-трапами (и в `README.md`, раздел «Exit codes») добавить одну строку:
«смерть по сигналу отдаётся как 128+N (130 INT, 143 TERM) и вердиктом кита не
является». Файлы: `scripts/kit-build.sh`, `scripts/probes-sync.sh`,
`tools/backup-divergence-probe.sh`, `tools/build-path-probe.sh`,
`tools/checks-on-image.sh`, `tools/fetch-corpus.sh`, `tools/lock-probe.sh`,
`tools/sweep.sh`, плюс `claude-patch-all.sh` и `tools/corpus-tools-bench.sh`
после F-2/F-3. `scripts/probes-sync.sh` уже в дереве со split-трапами.

## F-9 (средняя). Срыв применения мутации у стенда зондов = 1, а не 2

`tools/probe-bench.js:1736-1744`: `replaceExactlyOnce` бросает при пропавшем
якоре, `catch` печатает «ОТКАЗ», цикл едет дальше, в конце `blinded !== length`
→ `process.exitCode = 1`. Неприменённая мутация — измерение НЕ СОСТОЯЛОСЬ, это
класс 2. Сосед `corpus-tools-bench.sh` в круге 24 уже отделил этот класс, а
полоса E пронесла его в три стенда. Сделать так же: сорвавшееся применение →
код 2, доминирующий над счётом; арм назвать в шапке (`:5-9`).

## F-10 (средняя). Инструменты судьи: нет шапок, «мерить нечем» отдаётся как 1

Прецедент кита: `sys.exit('строка')` даёт код 1, даже если шапка обещала 2.

| файл | достижимо сегодня | нужно |
|---|---|---|
| `judge/replay.py:27,38` | `SystemExit('образ не прочитан…')`, `'словарь вердиктов не извлечён…'` → 1 | 2 |
| `judge/adjudicate.py:243` | `SystemExit('--jobs…')` → 1 | 2 (контракт вызова) |
| `judge/adjudicate.py:247` | `SystemExit('записи не найдены')` → 1 | 5 (нечего мерить) |
| `judge/validate.py:514,534,639-643` | то же (prompt/записи/truth/repeat/jobs) → 1 | 2 для контракта, 5 для «записей нет» |
| `judge/compact.py` | argparse → 2, штатный конец 0 | шапка |

СДЕЛАТЬ: завести шапку кодов в каждом из четырёх файлов (подмножество общей
таблицы) и перевести перечисленные выходы на нужный код. Распределение
«контракт вызова → 2, записей нет → 5» — решение контроллера, не обсуждается.

## F-11 (низкая). Нет шапки кодов у двух питоновских инструментов

`set-model-costs.py` достижимо выходит 0/1 (`sys.exit(main())` на `:685`), при
этом `README.md:337-338` пишет «never exits fatally» — это правда КОНВЕЙЕРА
(`claude-patch-all.sh:5560-5567` глотает ненулевой как WARNING), а не скрипта.
`claude_patch.py:70-72` `die()` всегда `sys.exit(1)`, включая нарушение
контракта вызова (нет файла), где класс — 2.

СДЕЛАТЬ: шапка кодов в обоих; `die()` получает необязательный код с умолчанием
1, контрактные отказы переводятся на 2; формулировку README поправить на «его
ненулевой код конвейер глотает как WARNING», чтобы утверждение принадлежало
тому, о ком оно верно.

## F-12 (средняя). Живое протухшее число, которое гейт чисел не видит по устройству

`claude-patch-all.sh:4948` — `EXPECTED_CHECKS = 118`; `:4959` — «Реестр выше
говорит, что все 114 сошлись НА СОБРАННОМ образе». Речь об ОДНОМ И ТОМ ЖЕ
реестре. ПРОВЕРЕНО КОНТРОЛЛЕРОМ: 118 и 114 стоят в девяти строках друг от
друга. Гейт 0d слеп здесь по устройству: существительное элидировано («все 114
сошлись»), владельца по грамматике не за что зацепить.

СДЕЛАТЬ ОБЕ ЧАСТИ:
  (а) починить число — 114 → 118, и переписать фразу так, чтобы существительное
      стояло явно и владелец читался («все 118 проверок реестра сошлись…»);
  (б) закрыть класс в САМОМ гейте: форма «вс(е|ех) <число>» с опущенным
      существительным обязана требовать владельца или явной пометки
      `docnum:*`, как и обычная форма. Аудитор измерил размер класса по всему
      киту: пять вхождений, четыре — про игрушечные «2 версии» в стенде.
      Добавить мутацию в `tools/docnum-mutations.tsv` на эту форму и поднять
      объявленный счётчик мутаций гейта чисел; число обязано стоять рядом с
      именем `docnum-bench`.

## F-13 (высокая, НОВАЯ). Боевой гейт PYCOMPILE слеп к форме `<<'ТЕГ'; then`

`claude-patch-all.sh:958` — якорь:

    opener = re.compile(r"^[^#]*\bpython3\b[^|;&]*<<'([A-Za-z_][A-Za-z0-9_]*)'\s*$")

требует, чтобы строка КОНЧАЛАСЬ открытием heredoc. ПРОВЕРЕНО КОНТРОЛЛЕРОМ:
`tools/build-path-probe.sh:1018` несёт `if ! python3 - "$PIPELINE" "$mut" <<'MUTX'; then`
— под якорь не подходит, и это питоновское тело не проверяется гейтом НИКОГДА.
Гейт при этом объявляет о себе, что перечисляет ВСЕ питоновские heredoc'и
скрипта — квантор ложен.

СДЕЛАТЬ: расширить якорь так, чтобы допускались хвосты вида `; then`, `; do`,
`; fi` и подобные управляющие продолжения, СОХРАНИВ защиту, ради которой стоял
`$`: упоминания слов внутри кода и комментариев (см. комментарий `:952-957` —
он объясняет, почему `$` там появился, и эту защиту терять нельзя). Проверить
результат на обеих формах: `build-path-probe.sh:1018` обязан теперь попадать в
перечень, а строки-примеры из комментариев — нет. Завести мутацию, которая
краснит гейт на пропущенной форме.

## F-14 (средняя, НОВАЯ). Зонд пути правит жертву без проверки разбора

`tools/build-path-probe.sh:867,1018` — та же свободная текстовая замена в
исполняемую жертву, что закрыта полосой E у пяти стендов. Пронести ту же
проверку: после замены жертва обязана разбираться своим разборщиком
(`bash -n` + питоньи heredoc-тела для `.sh`, `py_compile` для `.py`), провал —
код 2. Функции извлечения и разбора уже написаны полосой E в
`tools/corpus-tools-bench.sh` (`python_heredoc_bodies`, `sh_victim_parses`) —
переиспользуй их форму, не изобретай вторую.

## F-15 (низкая, НОВАЯ). Динамическая область видимости в стенде раскатки

`tools/probes-sync-bench.sh`: строка вида
`local root="$1" n="$2" file="$root/…"` работает в bash 3.2 только потому, что
раскрытие `$root` берёт локаль ВЫЗЫВАЮЩЕГО; при вызове с верхнего уровня даёт
`root: unbound variable`. Разделить объявление и присваивание.

## Приёмка

Гейты СЫРЫМИ кодами, без пайпа (`cmd > log 2>&1; echo $?`; форма
`cmd | tail; echo $?` показывает код хвоста и ЗАПРЕЩЕНА). Прогонять
ПОСЛЕДОВАТЕЛЬНО: сценарии судейского стенда зовут настоящий
`scripts/probes-sync.sh` с живым замком, и два параллельных прогона дерутся за
него (замерено полосой E).

    bash tools/corpus-tools-bench.sh              -> 0, сценариев=102 расхождений=0
    bash tools/corpus-tools-bench.sh --self-check -> 0, мутаций=106 покраснели=106
    python3 tools/docnum-bench.py                 -> 0, счётчик после F-12(б) поднят
    python3 tools/judge-tools-bench.py            -> 0, сценариев=36 расхождений=0
    python3 tools/judge-tools-bench.py --self-check -> 0, мутаций=43 покраснели=43
    python3 tools/costs-bench.py                  -> 0 и --self-check -> 0
    bash tools/probes-sync-bench.sh               -> 0 и --self-check -> 0
    bash tools/lock-probe.sh                      -> 0
    bun tools/probe-bench.js --binary /Users/maratkarimov/.local/share/claude/versions/2.1.250 -> 0, сценариев=66 расхождений=0
    bun tools/probe-bench.js --self-check --binary <тот же> -> 0

КАЖДАЯ правка кода возврата обязана быть закрыта НАЗВАННОЙ мутацией или
прогоном, воспроизводящим прежнее поведение: для F-1 это прогон с
несуществующим каталогом замка (ждём 6, было 3); для F-2/F-3 — реплика TERM,
показывающая 143 БЕЗ строки «ошибка оболочки»; для F-4/F-6 — прогон ребёнка,
возвращающего 2 и 4, с проверкой кода вызывающего. Утверждение без названного
доказательства не принимается.

ЗАПРЕТЫ: НЕ запускать `claude-patch-all.sh` и НЕ запускать `tools/sweep.sh` без
аргументов — это настоящие сборки на 35+ минут с сетью. Правку `claude-patch-all.sh`
проверять `bash -n` и вырезанием нужного гейта, как это делает `docnum-bench.py`.

ЛОВУШКА (замерено): правка исполняемого bash-скрипта РВЁТ текущий прогон — bash
читает тело по СМЕЩЕНИЮ. Не редактируй файл, исполняемый фоновым прогоном.
ВТОРАЯ ЛОВУШКА (замерено): BSD `seq 1 0` печатает «1 0» ВНИЗ, а не пустоту —
пустой диапазон надо гейтить явно.

СТОП-ПРАВИЛО: неожиданное падение или расхождение с брифом — одна честная
попытка, потом BLOCKED с сырым выводом, без глубокой диагностики.
ПРАВО ОТКАЗА: пункт на ложной посылке — доказательство (grep/diff/прогон) и
стоп. Фабриковать дифф запрещено.

ОТЧЁТ в ответе: status / правки по F-1..F-15 / счётчики ДО и ПОСЛЕ / названные
доказательства каждой правки кода / adjudication requests / вне-скоуп.

<!-- BRIEF COMPLETE -->
