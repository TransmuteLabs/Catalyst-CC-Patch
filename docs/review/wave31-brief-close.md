# Волна 31, бриф 4 (закрывающий) — K-11 и находка контроллера W-1

Дерево: `c04e0fa`. Два предмета, оба остались от круга 26: K-11 из
`docs/review/round26-lens-K.md` и W-1 из `docs/review/findings-ledger.md`
(находка контроллера, найдена на приёмке волны).

**Скоуп записи (`paths:`):** `tweakcc-patch.js`, `tools/sweep.sh`,
`tools/probe-bench.js`, `tools/corpus-tools-bench.sh`. Брифы 1-3 этой волны уже
закоммичены — их правки не трогать и не «улучшать».

**Коммитов НЕ делать.** Изменения остаются в рабочем дереве.

**Все решения приняты. Открытых вопросов ноль.** Цитата не совпала с деревом →
BLOCKED с сырым `grep -n`. Нашёл внутреннее противоречие в брифе — останавливайся
и называй его, не выбирай сторону сам (на брифе 2 этой волны такое было, и
остановка оказалась правильным ходом).

**Комментарии** — как в патче: полные, с причиной.

---

## 1. Негодный образец фильтра не имеет права стать «не совпало» (K-11)

Сейчас (`tweakcc-patch.js:2849`):

    '__mt=(__l,__s)=>Array.isArray(__l)&&__l.length>0&&__l.some((__r)=>{try{return new RegExp(__r).test(__s)}catch{return !1}});' +

Негодный образец ловится `catch` и молча становится «не совпало». Замер аудитора:
`filter.classes_judge = ["1e("]` (незакрытая группа) — и при непустом списке
судья не консультируется НИ ПО ОДНОМУ вызову, журнал пишет `filtered`, а не
поломку. Снаружи судья «работает», по делу мимо всех.

**ВНИМАНИЕ, тёзка.** В файле есть ВТОРОЙ `__mt` — локальная функция маркера
внутри `__cut` (`:3077`, `:3078`, `:3088`). Это ДРУГАЯ функция в другой области
видимости, её НЕ ТРОГАТЬ. Правится только фильтрующий `__mt` со строки 2849 и
его три вызова на `:2852`, `:2853`, `:2856`.

Получить сигнатуру `__mt=(__k,__l,__s,__safe)`, где `__safe` — что вносит в
`.some()` НЕГОДНЫЙ образец, плюс однократное объявление на ключ через
`__deg`/`__nseen` в форме
`"bad-setting:"+__k+"="+__clip(__r,24)+" (need regexp), считаем "+(__safe?"совпадением":"несовпадением")`.

Безопасная сторона у двух семейств РАЗНАЯ, и в обоих случаях она означает
«консультацию провести»:

* `:2852` `classes_skip` и `:2853` `agents_skip` — совпадение значит ПРОПУСТИТЬ
  судью. Негодный образец обязан вносить `!1` (не совпало) — иначе поломка
  образца сама себе выписывает пропуск.
* `:2856` `classes_judge` и `agents_judge` — совпадение значит СУДИТЬ. Негодный
  образец обязан вносить `!0` — иначе поломка образца отменяет судью для всех.

Ключ в первом аргументе — имя списка (`"classes_skip"`, `"agents_skip"`,
`"classes_judge"`, `"agents_judge"`), чтобы объявление называло, ГДЕ образец.

## 2. Зубы реестра меряют собранный образ прогона, а не установленный (W-1)

Сейчас стадия зубов (`tools/sweep.sh`, блок вокруг строки 823 —
`python3 "$TEETH_PY" 8>&- > "$TEETH_LOG" 2>&1`) зовёт прибор БЕЗ `--image`, а
умолчание прибора — `~/.local/bin/claude` (`tools/checks-teeth.py:261`), то есть
УСТАНОВЛЕННЫЙ образ. Свип при этом строит свои: каждая версия собирается в
`"$STATE/bin/$v.wave.bin"` (`tools/sweep.sh:1061`, ключ `--target`).

Замерено контроллером на дереве `e9b172b`:
`bash tools/checks-on-image.sh ~/.local/bin/claude` → **107 [OK] / 12 [FAIL]**.
Двенадцать красных — ровно те, чьи формы поменяла эта волна. Контроль прибора
(«названный образ обязан быть зелёным до мутаций») проверял чужой артефакт, и
после ЛЮБОЙ волны, меняющей запиненные формы, стадия не может мерить по
устройству — до переустановки.

Получить:

* Стадия зубов переносится и исполняется ПОСЛЕ первой УСПЕШНО собранной версии
  и получает её образ: `python3 "$TEETH_PY" --image "$STATE/bin/$v.wave.bin"`.
  Прибор мутирует КОПИЮ (`tools/checks-teeth.py:249`, `shutil.copyfile`), сам
  файл только читается — сборке это не мешает, проверено контроллером.
* Ручка `SWEEP_SKIP_CHECKS_TEETH` и все шесть рук разбора кода возврата
  (0/5/3/2/4/6) сохраняются дословно, включая тексты и `sum_line`.
* Появляется седьмая ситуация: ни одна версия не собралась ⇒ до стадии зубов
  дело не дошло. Свип обязан сказать это ОТДЕЛЬНОЙ строкой и пометкой в сводке
  («зубы реестра: НЕ ИЗМЕРЕНЫ -- ни одна версия не собралась»), а не молчать:
  молчание здесь неотличимо от зелёного.
* В коде отказа контроля причина называется СВОИМ именем. Сейчас свип печатает
  дизъюнкцию «якорь мутации, длина замены или контроль» — из неё оператору не
  видно, какая из трёх сработала. Разделить: код 2 с провалом контроля получает
  строку «образ прогона красен ещё до мутаций — реестр расходится с тем, что
  собрал этот прогон», прочие причины кода 2 остаются как есть.

**Цена, которую надо назвать в комментарии:** прежде стадия падала ДО сборок и
ловила беззубый реестр за секунды; теперь одна версия собирается раньше
проверки. Это осознанный размен: прибор обязан мерить артефакт, который прогон
и производит, а не соседний.

---

## Зубы

Каждое утверждение — с названной мутацией, красящей СВОЙ сценарий СВОЕЙ
причиной. Мутант, падающий с ошибкой разбора/сборки, покраснением НЕ считается.

Дом сценариев K-11 — `tools/probe-bench.js`; дом сценариев W-1 —
`tools/corpus-tools-bench.sh` (там уже живут сценарии отказов свипа).
Новых стендов не заводить. Если правка меняет объявленные счётчики
(`EXPECTED_SCENARIOS`/`EXPECTED_MUTATIONS` и их объявления в доках), догони их и
назови в отчёте; проверка без пайпа:
`sed -n '1343,2181p' claude-patch-all.sh > /tmp/dg.py && python3 /tmp/dg.py claude-patch-all.sh; echo EXIT=$?`

Обязательные сценарии:

1. `filter.classes_judge = ["1e("]` → консультация ПРОВОДИТСЯ, в `__deg` есть
   `bad-setting:classes_judge=1e( (need regexp), считаем совпадением`.
2. `filter.classes_skip = ["1e("]` → пропуска НЕТ, консультация проводится,
   объявление говорит «считаем несовпадением».
3. Годный образец в обоих списках работает как прежде (положительный контроль:
   без него правка «всё судить» прошла бы зелёной).
4. Свип: первая версия собралась → зубы позвались с `--image` на
   `$STATE/bin/<версия>.wave.bin` (а не на `~/.local/bin/claude`).
5. Свип: ни одна версия не собралась → отдельная строка и пометка в сводке, а
   не тишина.
6. Свип: контроль прибора провалился → причина названа своим именем, а не
   дизъюнкцией трёх.

## Гейт до отчёта

`node --check tweakcc-patch.js`, `bash -n tools/sweep.sh`, оба стенда с точными
счётчиками ДО и ПОСЛЕ, гейт чисел. Полный конвейер и полный свип НЕ запускать —
это делает контроллер.

Подтверждение гейта — БЕЗ пайпа либо с `set -o pipefail`.

## Контракт отчёта

Раздел `## ОТЧЁТ ИСПОЛНИТЕЛЯ` в конец ЭТОГО файла: по каждому из двух предметов
`file:line` после правки, дословная новая форма, имя мутации и покрасневший
сценарий; счётчики стендов ДО→ПОСЛЕ; какие объявленные числа догнал; отдельным
списком — найденное вне скоупа.

**Стоп-правило:** неожиданное падение или расхождение с брифом → одна честная
попытка → BLOCKED с сырым выводом. Глубокой диагностики не вести.

**Право на отказ:** правка уже в дереве или посылка ложна → доказательство и
стоп по пункту. Диффы не выдумывать.

<!-- BRIEF COMPLETE -->

## ОТЧЁТ ИСПОЛНИТЕЛЯ

Статус: DONE. Оба предмета закрыты. Коммитов нет.

### Счётчики стендов ДО -> ПОСЛЕ

Дословные строки ИТОГ/SELF-CHECK/EXIT= из прогонов:

- ДО probe-bench: `probe-bench: ИТОГ сценариев=79 расхождений=0` EXIT=0
- ДО corpus-tools-bench: `corpus-tools-bench: ИТОГ сценариев=110 расхождений=0` EXIT=0
- RED probe-bench (стенд новый, код старый): `probe-bench: ИТОГ сценариев=82 расхождений=2` EXIT=1
- RED corpus-tools-bench: `corpus-tools-bench: ИТОГ сценариев=113 расхождений=3` EXIT=1
- ПОСЛЕ probe-bench: `probe-bench: ИТОГ сценариев=82 расхождений=0` EXIT=0
- ПОСЛЕ probe-bench --self-check: `probe-bench: SELF-CHECK мутаций=5 ослепили=5` EXIT=0
- ПОСЛЕ corpus-tools-bench: `corpus-tools-bench: ИТОГ сценариев=113 расхождений=0` EXIT=0
- ПОСЛЕ corpus-tools-bench --self-check: `corpus-tools-bench: SELF-CHECK мутаций=127 покраснели=127` EXIT=0

RED probe: 2 из 3 новых (положительный контроль `filter-valid-lists-still-discriminate` зелёный и на старом коде). RED corpus: 111, 112, 113.

Гейт: `node --check tweakcc-patch.js` EXIT=0; `bash -n tools/sweep.sh` EXIT=0; docnum `ЧИСЛА В ДОКАХ СОВПАДАЮТ С ОБЪЯВЛЕННЫМИ` (probe-bench scenarios=82 mutations=5; corpus-tools-bench scenarios=113 mutations=127) EXIT=0.

Образ для probe-bench -- adhoc-patch `--script @tweakcc-patch.js` на копию 2.1.251.orig. Установленный бинарник не трогался.

### 1. K-11 -- негодный образец фильтра

`tweakcc-patch.js:2858-2870` (фильтрующий `__mt`; тезка внутри `__cut` на `:3079` не тронут).

Новая форма:

    __mt=(__k,__l,__s,__safe)=>Array.isArray(__l)&&__l.length>0&&__l.some((__r)=>{try{return new RegExp(__r).test(__s)}catch{if(!__nseen[__k]){__nseen[__k]=1;__deg.push("bad-setting:"+__k+"="+__clip(__r,24)+" (need regexp), считаем "+(__safe?"совпадением":"несовпадением"))}return __safe}});

Вызовы: `:2866` classes_skip / !1; `:2867` agents_skip / !1; `:2870` classes_judge / !0 и agents_judge / !0.

Журнальный текст (после u-раскрытия, как у соседнего live_kinds): `bad-setting:<ключ>=<образец> (need regexp), считаем совпадением|несовпадением`.

Сценарии probe-bench:
- `filter-bad-regexp-judge-still-consults` -- RED: outcome=filtered, канал=0; ПОСЛЕ: ok, канал=1, degExact совпадением.
- `filter-bad-regexp-skip-does-not-skip` -- RED: ok/канал=1 без deg; ПОСЛЕ: degExact несовпадением.
- `filter-valid-lists-still-discriminate` -- зелёный на RED и на ПОСЛЕ (годный skip=1c + judge=1c при классе 1e -> not_in_judge_list). Существующий `filtered` пинит годный skip=1e.

Мутации harness probe-bench: 5/5 ослепили.

Один авто-фикс в своём коде: первая сборка образа с сырой кириллицей в инъекции дала порчу на latin1-карве. Перевод в \\uXXXX, как у live_kinds :3740. Повторный прогон -- 82/0.

### 2. W-1 -- зубы меряют образ прогона

tools/sweep.sh:
- :881-905 -- предполёт: ручка SWEEP_SKIP_CHECKS_TEETH и отказ «нет прибора» с прежними текстами; замер убран. Комментарий называет размен (сборка раньше проверки).
- :1147-1188 -- после первой сборки (rc!=3 и есть $STATE/bin/$v.wave.bin), до сноса зелёной копии: python3 "$TEETH_PY" --image "$STATE/bin/$v.wave.bin". Шесть рук 0/5/3/2/4/6 сохранены, включая тексты и sum_line.
- :1162-1171 -- рука 2 разделена: grep -q 'КОНТРОЛЬ ПРОВАЛЕН' -> «образ прогона красен ещё до мутаций -- реестр расходится с тем, что собрал этот прогон»; иначе прежняя дизъюнкция «якорь мутации, длина замены или контроль».
- :1206-1209 -- седьмая ситуация: `SWEEP зубы реестра: НЕ ИЗМЕРЕНЫ -- ни одна версия не собралась` и та же пометка в сводке.

Сценарии corpus-tools-bench и мутации 125-127:
- 111 -- --image на bin/900.wave.bin, не ~/.local/bin/claude. Мутация 125 (убрать --image) -> НЕТ_--image.
- 112 -- нет сборки (chmod 500 на bin): отдельная строка и сводка. Мутация 126 (echo -> :) -> ЗУБЫ_МОЛЧАТ.
- 113 -- STUB_TEETH_CONTROL_FAIL: своё имя, не дизъюнкция. Мутация 127 (КОНТРОЛЬ ПРОВАЛЕН -> КОНТРОЛЬ НИКОГДА) -> ДИСЪЮНКЦИЯ.

Мутация 78 перенацелена: PAT `2) echo "SWEEP ОТКАЗ: прибор зубов НЕ МЕРИЛ` больше не встречается (рука стала `2) if grep -q`); PAT/REP -> `2) if grep -q` / `22) if grep -q`. Сценарий 78 по-прежнему краснеет классом 2->default.

### Объявленные числа, которые догнал

- tools/probe-bench.js: EXPECTED_SCENARIOS 79->82; poison self-check 82->81.
- tools/corpus-tools-bench.sh: EXPECTED_SCENARIOS 110->113; EXPECTED_MUTATIONS 124->127.
- README.md: «110 scenarios»->113, «124 recorded mutations»->127 (вне paths:, бриф велел догнать объявления; без этого docnum красный).
- claude-patch-all.sh:5559: «probe-bench's 79 scenarios»->82 (то же).

### Вне скоупа

- README.md и claude-patch-all.sh -- только числа, см. выше.
- Проза README про registry-teeth «a red instrument stops the sweep before the first build» устарела (теперь после первой сборки). Не переписывал гигантский абзац: это не счётчик.
- Установленный образ / реестр 12 FAIL -- ожидаемое, не трогал.

### Adjudication requests

1. «Успешно собрана» = конвейер дошёл (rc!=3) и $STATE/bin/$v.wave.bin на месте. Красная по полям версия всё равно даёт образ этого прогона; зубы зовутся по ней, если она первая.
2. Ручка пропуска осталась на предполёте (не мерит чужой образ); шесть рук уехали вместе с python3 --image.
3. Седьмая ситуация не меняет код возврата: она сопутствует уже RED/UNMEASURED.

### Self-Check: PASSED

Файлы на месте (tweakcc-patch.js, tools/sweep.sh, tools/probe-bench.js, tools/corpus-tools-bench.sh). Счётчики -- дословный вывод прогонов выше. Статус DONE согласован с телом (неожиданного красного нет; 12 FAIL установленного образа -- объявленное брифом). Коммитов нет.
