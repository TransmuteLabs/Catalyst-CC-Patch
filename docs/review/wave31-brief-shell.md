# Волна 31, бриф 2 из 3 — ОБОЛОЧКИ (`tools/sweep.sh`, `claude_patch.py`, `scripts/probes-sync.sh`, `claude-patch-all.sh`)

Дерево: `07773c9`. Находки: `docs/review/round26-lens-K.md` (K-4, K-10),
`docs/review/round26-lens-L.md` (L-1, L-2, L-7, L-8). Адъюдикация контроллера —
хвост `docs/review/findings-ledger.md`.

**Скоуп записи (`paths:`):** `tools/sweep.sh`, `tools/lock-probe.sh`,
`tools/build-path-probe.sh`, `claude_patch.py`, `scripts/probes-sync.sh`,
`claude-patch-all.sh` и стенды в `tests/`. **`tweakcc-patch.js` НЕ ТРОГАТЬ —
по нему одновременно идёт другой исполнитель.** Замеченное вне скоупа — строкой
в отчёт.

**Коммитов НЕ делать.** Изменения остаются в рабочем дереве; коммитит
контроллер после личной сверки.

**Все решения приняты. Открытых вопросов ноль.** Цитата не совпала с деревом →
BLOCKED с сырым `grep -n`, без догадок.

**Комментарии** — как в патче: полные, с причиной. Существующие не удалять;
описывающие меняемое поведение — переписать под новый инвариант.

---

## 1. `SWEEP_LAST_N` — десятичная база И запрет пустого набора (K-4)

Сейчас (`tools/sweep.sh:543-556`) `case` пропускает любую строку из цифр, а
арифметика bash восьмеричная. Под `set -u` без `set -e`: `SWEEP_LAST_N=08` →
`(( 08 == 0 ))` падает, ветка «весь корпус» не берётся, срез падает, `SRC`
пуст — и хвост (`:1143`) печатает **«SWEEP DONE: все 0 версий измерены, красных
нет», код 0**. `0010` даёт восьмеричные 8 версий, а объявление несёт `0010`.

Правка A — база. Тот же кит уже умеет правильно:
`validated_nonnegative_integer` в `claude-patch-all.sh:5238` снимает ведущие
нули, ловит переполнение и считает `$((10#$digits))`. Перенести ЭТОТ алгоритм в
`sweep.sh` дословно (общий файл-библиотеку НЕ заводить: списки копирования кита
пришлось бы править в нескольких местах, и пропуск одного убивает конвейер).
В обеих точках — комментарий-перекрёстка: «вторая копия правила живёт в
<файл:строка>; расхождение ловится сценарием стенда, а не чтением».

Правка B — корень, а не симптом. Пустой набор версий обязан быть ОТКАЗОМ, а не
зелёным хвостом. Сразу после формирования `SRC`:

    if (( ${#SRC[@]} == 0 )); then
      echo "SWEEP ОТКАЗ: набор версий пуст -- мерить нечего" >&2
      exit 5
    fi

Код 5 — по таблице кодов в шапке файла («нечего мерить»). Эта проверка стоит
независимо от того, чем набор опустел: она закрывает и будущие пути, а не только
восьмеричный.

## 2. Снос обломка — только через примитив ожидания (L-1)

Сейчас `rm -rf "$__stale"` (`:386-391`) исполняется РАНЬШЕ проверки живых
процессов (`:446-459`). Конвейер намеренно запускается без дескриптора замка
(`:983-984`, `8>&-`), поэтому осиротевший `claude-patch-all.sh` штатно не держит
ничего: следующий свип берёт свободный замок и сносит каталог из-под живого
ребёнка. Измерено аудитором: каталог удалён, процесс жив, и лишь ЗАТЕМ отказ 3.

В этом же файле уже есть правильный примитив — `__drop_kit_when_idle`
(`:149`). Получить:

* `__drop_kit_when_idle [путь] [бюджет]`: путь по умолчанию `$HERE`, бюджет по
  умолчанию `${SWEEP_KIT_DRAIN:-60}`.
* `__GUARD_KIT=0` снимается ТОЛЬКО когда путь равен `$HERE` (флаг принадлежит
  своему снимку, чужой обломок к нему отношения не имеет).
* Проверка жильцов (`ps -eo pid,args` + `index($0, path)`) стоит непосредственно
  перед `rm -rf` — между ними ничего.
* Чужие обломки идут с бюджетом **0**: одна проверка, без ожидания. Ждать за
  мёртвый прогон не наше дело; снести из-под живого — наше.
* Жильцы найдены → каталог остаётся, печатается существующая строка «снимок кита
  НЕ убран», прогон продолжается.

Цикл прополки (`:386-391`) переводится на этот примитив для `kit.??????` **и**
`sweep.self.??????` (из обоих ИСПОЛНЯЮТСЯ процессы). `sweep.pgid` — файл данных,
его никто не исполняет: остаётся прямой `rm -f`, и комментарий обязан назвать,
почему эти двое разные.

## 3. Цель патчера выбирается по ДОПУСТИМОЙ форме имени (L-2)

Сейчас (`claude_patch.py:301-307`) отбор идёт вычитанием суффиксов:

    skip = (".orig", ".staging", ".restore", ".repair", ".new", ".tmp", ".bak", ".download")
    cands = [p for p in vdir.iterdir() if p.is_file() and not p.name.endswith(skip)]
    return max(cands, key=lambda p: p.stat().st_mtime)

Отрицательный список не знает имён, которых ещё не придумали. Собственный
`tempfile.mkstemp(prefix=".claude-patched-", dir=target.parent, ...)` (`:539-540`)
под него не подпадает — и он САМЫЙ СВЕЖИЙ по mtime. Умерший до `os.replace`
процесс оставляет файл, который второй запуск выбирает целью, видит в нём наш
маркер и печатает «Already patched — nothing to do.», не тронув установленный
бинарник (измерено аудитором на изолированном каталоге).

Получить положительное правило: кандидат — файл, чьё имя ЦЕЛИКОМ совпадает с
`^\d+\.\d+\.\d+$` (на Windows — то же плюс суффикс `.exe`). Живой каталог версий
сегодня содержит ровно `2.1.250 2.1.250.orig 2.1.251 2.1.251.orig` — правило
берёт две настоящие версии и ни одного обломка.

Кандидатов нет → существующий `die(..., code=2)`, но причина названа явно:
в каталоге нет файла с ИМЕНЕМ ВЕРСИИ, а обломок оборванного прогона целью не
бывает. Тупой отказ здесь дешевле молчаливого успеха над остатком.

Кортеж `skip` удаляется; на его месте — комментарий, фиксирующий, ПОЧЕМУ
отрицательный список брошен (его нельзя дописать до полноты; предыдущая правка
дописывала в него `.download`, и следующая дописывала бы `.claude-patched-`).

## 4. Промежуточное имя принадлежит писателю (L-7)

`_replace_backup` (`claude_patch.py:469-480`) стажирует в фиксированное
`<backup>.new`. Два самостоятельных запуска делят один inode: измерено, что B
переименовал файл, пока A писал в уже открытый дескриптор, и опубликованный
backup собрался из половин обоих.

Параллельный прямой запуск — ПОДДЕРЖИВАЕМАЯ поверхность: страж живых процессов в
`sweep.sh` существует ровно потому, что «оператор вправе собирать в соседнем
окне».

Получить: `tmp = backup.with_name(backup.name + f".new.{os.getpid()}")`.

Дальше — мехнический свип по тому же правилу: в `claude_patch.py` найти
(`grep -n 'with_name\|"\.new"\|\.tmp\b'`) КАЖДОЕ фиксированное промежуточное имя,
которое могут разделить два процесса, и добавить в него `os.getpid()`. Правило
закрытое: фиксированное стажирующее имя ⇒ `.<pid>`. Все найденные имена
перечислить в отчёте.

Правки 3 и 4 обязаны лечь ВМЕСТЕ: `.new.<pid>` не ловится суффиксным
`endswith(".new")`, поэтому починка одной в одиночку открывает новый класс
остатка в другой.

## 5. Осиротевшая стадия `probes-sync` — владелец, а не номер (L-8)

`prune_one_sync_stage` и `report_one_sync_stage` (`scripts/probes-sync.sh:294-319`)
проверяют только `kill -0 <pid>`. Номер принадлежит УЖЕ УМЕРШЕМУ писателю: если
номер достался постороннему долгоживущему процессу, многолетняя стадия считается
активной записью. Измерено: stage с mtime 2020 года и PID постороннего `sleep` —
`--diff` вернул 0 со строкой «стадия живого писателя», `--to-home` оставил файл.

В ЭТОМ ЖЕ ФАЙЛЕ каталог-замок уже сверяет PID вместе со временем старта
(`:243-246`, формат `pid<TAB>lstart`, пустая метка = решение по `kill -0`).

Получить: та же дисциплина у стадии.

* Писатель ДО создания стадии пишет файл-владелец
  `<файл>.sync-owner.<pid>` со строкой `pid<TAB>$(LC_ALL=C ps -o lstart= -p $$)`.
  Имя намеренно с ДРУГИМ инфиксом: `for_each_sync_stage` глобит
  `"${PAIR_B[$i]}.sync-new."*`, и сосед с суффиксом `.owner` попал бы в этот
  глоб как ещё одна стадия.
* Живым писателем стадия считается, только если владелец существует, номер жив
  И `lstart` совпадает. Нет файла-владельца при существующей стадии ⇒ мёртвый
  (порядок записи это гарантирует).
* Прополка сносит стадию вместе с её файлом-владельцем.

## 6. Одна истинность у env-ручек кита (K-10)

Замерено аудитором: три языка в одном ките — `CLAUDE_PATCH_SKIP_MODELS` строго
`== "1"` (`claude-patch-all.sh:5731`), `SWEEP_SKIP_*` и `KEEP_ROOT` по `-n`.
Отсюда: `SKIP_MODELS=true` синхрон цен НЕ пропускает (меняет измеряемую свежесть
цен без объявления), `SWEEP_SKIP_BUILD_PROBE=0` зонд ПРОПУСКАЕТ, `KEEP_ROOT=0`
корень ОСТАВЛЯЕТ.

Получить общий читатель, вписанный в каждый из файлов, где он нужен (общую
библиотеку не заводить — причина та же, что в правке 1), с комментарием-
перекрёсткой между копиями:

    # Значения-истина: 1 true yes on (без учёта регистра). Ложь: пусто,
    # отсутствие, 0 false no off. Всё прочее -- ОТКАЗ кодом 2 с именем ручки:
    # в оболочке отказ дёшев и громок, а тихо выбранная сторона у ручки,
    # меняющей измеряемое, -- это ровно тот дефект, который здесь чинится.
    # (В ядре, tweakcc-patch.js, та же семья решена иначе -- безопасная сторона
    # плюс строка в журнал: там отказ убил бы живую сессию человека.)
    __envon() { ... }

Перевести: `CLAUDE_PATCH_SKIP_MODELS`, `SWEEP_SKIP_BUILD_PROBE`,
`SWEEP_SKIP_TOOLS_BENCH`, `SWEEP_SKIP_CHECKS_TEETH`, `KEEP_ROOT` (в
`tools/lock-probe.sh` И в `tools/build-path-probe.sh` — проверить обоих
`grep -rn 'KEEP_ROOT' tools/`). Затем `grep -rn '\-n "\${[A-Z_]*:-}"' tools/ scripts/ claude-patch-all.sh`
и перечислить в отчёте все прочие ручки этой семьи, найденные, но НЕ переведённые
(с причиной).

**Смена поведения, которую обязан назвать отчёт:** `SWEEP_SKIP_BUILD_PROBE=0`
теперь НЕ пропускает зонд. Это и есть исправление, а не побочный эффект.

---

## Зубы

Каждое утверждение — с названной мутацией, красящей СВОЙ сценарий СВОЕЙ
причиной. Мутант, падающий с ошибкой сборки/разбора, покраснением НЕ считается.

Дом сценариев — существующий стенд корпусных инструментов
(`grep -rl 'SWEEP_LAST_N\|corpus-tools-bench' tests/ tools/`), новый стенд не
заводить. Обязательные сценарии:

1. `SWEEP_LAST_N=08` на синтетическом корпусе из >=10 версий → ровно **8**
   версий (ведущий ноль -- запись числа, а не восьмеричная система). **И**
   отдельный сценарий: пустой набор версий (любой причиной) → код 5, а НЕ
   «все 0 версий измерены». **И** третий: `abc`, `-1`, `1.5`, пусто и
   переполнение (20 знаков) → отказ кодом 2 с именем ручки.
2. `SWEEP_LAST_N=0010` → ровно 10 версий, не 8.
3. Обломок `kit.XXXXXX`, из которого исполняется живой процесс, старше суток →
   каталог НА МЕСТЕ, строка «снимок кита НЕ убран», процесс жив.
4. Каталог версий с `.claude-patched-<хвост>`, новее настоящей версии →
   выбрана настоящая версия; каталог только из обломков → отказ 2 с названной
   причиной.
5. Два `_replace_backup` над одним backup → имена стадий различны (по `<pid>`);
   опубликованный backup побайтно равен ОДНОМУ из источников.
6. Стадия `probes-sync` с чужим переиспользованным номером (владелец с иным
   `lstart`) → `--diff` объявляет расхождение, прополка сносит стадию и владельца.
7. `CLAUDE_PATCH_SKIP_MODELS=true` → синхрон ПРОПУЩЕН; `SWEEP_SKIP_BUILD_PROBE=0`
   → зонд ИДЁТ; `KEEP_ROOT=0` → корень удалён; `KEEP_ROOT=ага` → отказ 2.

## Гейт до отчёта

Стенд(ы) с точными счётчиками ДО и ПОСЛЕ. `bash -n` на каждом правленом shell-файле.
Полный конвейер и свип НЕ запускать — это делает контроллер.

Форма подтверждения гейта: `set -o pipefail` перед пайпом либо код возврата без
пайпа. `команда | tail -N; echo $?` показывает код ХВОСТА и как подтверждение
не принимается.

## Контракт отчёта

Раздел `## ОТЧЁТ ИСПОЛНИТЕЛЯ` в конец ЭТОГО файла: по каждой из 6 правок
`file:line` после правки, дословная новая форма, имя мутации и покрасневший
сценарий; счётчики стендов ДО→ПОСЛЕ; отдельным списком — найденное вне скоупа и
непереведённые ручки с причиной.

**Стоп-правило:** неожиданное падение или расхождение с брифом → одна честная
попытка → BLOCKED с сырым выводом. Глубокой диагностики не вести.

**Право на отказ:** правка уже в дереве или посылка ложна → доказательство
(`grep -n`/дифф) и стоп по пункту. Диффы не выдумывать.

<!-- BRIEF COMPLETE -->

## ОТЧЁТ ИСПОЛНИТЕЛЯ

BLOCKED (own scope, undiagnosed; contradictory requirement in docs/review/wave31-brief-shell.md)

### Стоп-доказательство

После RED-first прогона внесена первая реализация. Обязательный сценарий 103 остался красным, потому что две дословные посылки брифа несовместимы: алгоритм, который велено перенести дословно, принимает `08` как десятичное 8, а тест-план требует отвергнуть то же значение кодом 2. Выбирать дополнительное правило, которого нет в брифе, исполнитель не вправе.

Сырой вывод:

```text
32-Правка A — база. Тот же кит уже умеет правильно:
33-`validated_nonnegative_integer` в `claude-patch-all.sh:5238` снимает ведущие
34:нули, ловит переполнение и считает `$((10#$digits))`. Перенести ЭТОТ алгоритм в
35-`sweep.sh` дословно
...
194:1. `SWEEP_LAST_N=08` → отказ кодом 2 с именем ручки.
195-   пустой набор версий (любой причиной) → код 5, а НЕ «все 0 версий измерены».
196-2. `SWEEP_LAST_N=0010` → ровно 10 версий, не 8.
```

Фактическая форма названного алгоритма (`claude-patch-all.sh:5266-5288`) снимает ведущий ноль циклом и затем считает `10#$digits`; для `08` результат — `8`, код 0.

### Состояние 6 правок

1. **K-4 — BLOCKED.** `tools/sweep.sh:562` получил десятичный валидатор, `tools/sweep.sh:600-604` — независимый отказ пустого `SRC` кодом 5. Сценарии: пустой набор зелёный; `0010` зелёный; `08` красный из-за противоречия выше. Финальная мутация не записана.
2. **L-1 — код и поведенческий сценарий зелёные.** `tools/sweep.sh:149` теперь имеет `__drop_kit_when_idle [путь] [бюджет]`; прополка `kit.??????`/`sweep.self.??????` зовёт его с бюджетом 0, `sweep.pgid` остаётся прямым `rm -f`. Сценарий 106 зелёный. Финальная мутация ещё не записана из-за стопа.
3. **L-2 — код и поведенческий сценарий зелёные.** `claude_patch.py:296` использует полное имя версии: `^[0-9]+\.[0-9]+\.[0-9]+$`, на Windows — то же с `.exe`; каталог только из обломков даёт существующий `die(..., code=2)` с названной причиной. Сценарий 107 зелёный. Финальная мутация ещё не записана.
4. **L-7 — код и поведенческий сценарий зелёные.** `claude_patch.py:477`: `backup.name + f".new.{os.getpid()}"`; механический свип дал ещё две формы: `claude_patch.py:629` и `:700`, `target.name + f".staging.{os.getpid()}"`. Сценарий 108 зелёный: два процесса получили разные имена, итог равен одному источнику. Финальная мутация ещё не записана.
5. **L-8 — код внесён, гейт не зелёный.** `scripts/probes-sync.sh:165-176` пишет `<dst>.sync-owner.<pid>` до стадии; `:310-322` требует owner + живой pid + совпавший `lstart`; `:327-329` удаляет стадию вместе с owner. Новый сценарий 6 зелёный, но прежние сценарии 2 и 5 ещё не переведены на создание owner и потому красные. Self-check не запускался.
6. **K-10 — код внесён, гейт не зелёный.** `__envon` добавлен в `tools/sweep.sh:742`, `claude-patch-all.sh:5776`, `tools/lock-probe.sh:65`, `tools/build-path-probe.sh:99`; вызовы переведены для пяти названных ручек. Фактическая половина `SWEEP_SKIP_BUILD_PROBE=0` сценария 109 зелёная, но проверка формы самого сценария ожидала `if __envon ...`, тогда как реализация хранит код результата отдельно для различения false=1 и invalid=2; сценарий остаётся красным. Финальные мутации не записаны.

Итого: код частично внесён по 6/6; поведенчески зелёны и не имеют текущего красного 3/6 (L-1, L-2, L-7). Завершённых по контракту с зубами и полным гейтом: 0/6.

### Стенды ДО → текущий ПОСЛЕ

- `tools/corpus-tools-bench.sh`: `102 сценария, 0 расхождений` → `109 сценариев, 2 расхождения`, EXIT 1.
- `tools/probes-sync-bench.sh`: `5 сценариев, 0 расхождений` → `6 сценариев, 2 расхождения`, EXIT 1.
- RED-first до кода был увиден: corpus `109/7`, EXIT 1; probes-sync `6/1`, EXIT 1.
- `bash -n` после первой реализации: зелёный для `tools/sweep.sh`, `tools/lock-probe.sh`, `tools/build-path-probe.sh`, `scripts/probes-sync.sh`, `claude-patch-all.sh`, а также обоих правленых стендов.
- Полный конвейер и свип не запускались по запрету брифа.

### Текущий точный diff working tree

```text
claude-patch-all.sh         +26 -1
claude_patch.py             +17 -18
scripts/probes-sync.sh      +45 -19
tools/build-path-probe.sh   +27 -2
tools/corpus-tools-bench.sh +219 -2
tools/lock-probe.sh         +26 -2
tools/probes-sync-bench.sh  +42 -5
tools/sweep.sh              +88 -19
ИТОГО                       +490 -68
```

Коммитов нет.

### Непереведённые совпадения `-n "${...:-}"`

- `tools/build-path-probe.sh`: `__CLI_HOLDER` — внутренний pid держателя, не env-выключатель.
- `tools/lock-probe.sh`: `HOLDER_KIDS`, `INHERIT_BODY`, `HOLDFIFO` — внутреннее состояние/fixture зонда, не операторские булевы ручки.
- `tools/sweep.sh`: `__PGID_OURS`, `STATE` — внутреннее состояние; `XDG_CONFIG_HOME` — стандартный путь, не булево значение.
- `tools/corpus-tools-bench.sh`: `ROOT` — внутренний путь стенда.
- `scripts/kit-build.sh`: `STAGE` — внутренний путь стадии; файл вне скоупа записи.
- `claude-patch-all.sh`: `CLAUDE_PATCH_LOCK_HELD_BY` — маркер наследования замка; `TWEAKCC_LOCAL`, `CLAUDE_PATCH_FLOOR_IMAGE` — пути; `XDG_CONFIG_HOME` — стандартный путь. Это не ручки семейства true/false.

### Вне скоупа

Ничего не изменено. `tweakcc-patch.js` не тронут. Созданный этой задачей `__pycache__/claude_patch.cpython-314.pyc` после синтаксической проверки удалён; существующие bytecode-файлы не тронуты.

### Что не сделано

- Не выбран ответ на противоречие `08`: принять как десятичное 8 либо отвергнуть кодом 2.
- Не завершены обновления прежних probes-sync fixtures под owner-файл.
- Не исправлена проверка формы сценария 109.
- Не добавлены все обязательные мутации corpus-tools и не прогнаны оба `--self-check`.
- Не выполнен финальный зелёный гейт и не сняты финальные номера строк/дословные формы для DONE-отчёта.
- Коммит не сделан.

Self-Check: PASSED — статус BLOCKED соответствует двум текущим красным стендам и противоречию брифа; созданные сценарии существуют, приведённые счётчики — дословные результаты реально запущенных команд.

---

## ПОПРАВКА КОНТРОЛЛЕРА №1 (после BLOCKED исполнителя)

Исполнитель нашёл в брифе противоречие и правильно остановился, а не выбрал
сторону сам: правка 1 требует ДОСЛОВНО перенести `validated_nonnegative_integer`
(он снимает ведущие нули и считает `08` десятичной восьмёркой), а сценарий зубов
№1 требовал на `08` отказ кодом 2. Одновременно выполнить нельзя.

**Решение: `08` — это ВОСЕМЬ, отказа нет.** Основания, а не вкус:

* канонический числовой читатель кита (`claude-patch-all.sh:5238`) уже трактует
  ведущие нули именно так, и вводить рядом ВТОРОЕ прочтение одной записи —
  это ровно тот дефект, который эта же волна чинит правкой 6 (три несовместимых
  «истинности» env в одном ките). Одна запись — одно значение по всему киту;
* отчёт линзы K сам указал на этот читатель как на образец («рядом тот же кит
  уже умеет иначе»), то есть находка была про ВОСЬМЕРИЧНОСТЬ, а не про
  недопустимость ведущего нуля;
* дефект, который ловила находка, — не «08 принято», а «08 стало нулём версий и
  прогон объявил зелёное». Его закрывает пара «десятичная база + отказ на пустом
  наборе», и обе половины остаются на месте.

Сценарий зубов №1 переписан выше. Отказ кодом 2 остаётся за настоящей
негодностью: `abc`, `-1`, `1.5`, пусто, переполнение.

Остальные пять правок и все прочие сценарии — без изменений.

<!-- BRIEF COMPLETE -->
