# wave165-shell-sentinel-C

Дорожка C: `tools/gate-kill-teeth.sh` + генерируемый `t4.sh`.
Статус: DONE

Приёмка §5.3 отменена контроллером, посылка правила опровергнута, замер:
`~/work/SIB/Transmutation/Nexus/Catalyst/programs/2026-09-16-shell-sentinel-165/MEASURE-trap-masking.md`.
Вместо §5.3: часовой краснит ветку `exit 0` без `__DONE=1`.

## 0. Гейт брифа

- `tail -1` брифа: `<!-- BRIEF COMPLETE -->` (позиционно в хвосте).
- Целевой файл не тронут предыдущей дорожкой: mtime 12:41, `__DONE` ноль вхождений (контроллер).
- uname: Darwin; date машины на старте: Wed Sep 16 16:37:36 MSK 2026.

## 1. ДО правки

sha256:
```
1e507fae7d02f83e4f8a618ae615a887900e456bb783c6a3468def354486a556  /Users/maratkarimov/work/SIB/Transmutation/Nexus/Catalyst/Catalyst-CC-Patch/tools/gate-kill-teeth.sh
```

`--self-check` вхождений: 0 (команда `grep -c -- '--self-check'`; grep -c на нуле дал rc=1, известная ловушка счётчика, не отказ файла).

Гейт форм ДО (падающий контроль; в выводе только наш файл — A/B уже зелёные):
```
ФОРМЫ ОБОЛОЧКИ, КОТОРЫЕ МОЛЧАТ:
  tools/gate-kill-teeth.sh: EXIT-трап без часового завершения -- обрыв на ошибке подстановки вернёт 0
GATE_RC=1
```

Зубы ДО (дословная итоговая строка прибора; `TEETH_BEFORE_RC=0`):
```
gate-kill-teeth: teeth=4 green=4 mutations=4 held=4
```

Пины: мутации в этом файле пинят текст ВЫРЕЗКИ хелперов (`mut.1`–`mut.4` old/new), не трап/cleanup самого файла. Команда: `grep -l 'gate-kill-teeth' tools/*.sh claude-patch-all.sh` — только `tools/gate-kill-teeth.sh`. Пинов на текст трапа нет, обновлять нечего.

## 2. Правка

Файл: `/Users/maratkarimov/work/SIB/Transmutation/Nexus/Catalyst/Catalyst-CC-Patch/tools/gate-kill-teeth.sh`

Часовой в ОБА скрипта (решение контроллера, не выбор):

- Родитель, верхний уровень, сразу после констант: `__DONE=0`, `WORKDIR=''`, `PIDS=''`, тело `cleanup()` переехало в гвард `__gate_kill_teeth_guard` (через вызов `cleanup`; `rm -rf` только при непустом WORKDIR, формой `if`, не `&&` — под `set -e` падение `[[ -n ]]` оборвало бы гвард). Трап: `trap '__gate_kill_teeth_guard' EXIT` плюс раздельные `INT`/`TERM` (130/143).
- Генерируемый `t4.sh` внутри `cat > … <<EOF` (EOF без кавычек): `__DONE=0`, `__t4_guard`, `trap '__t4_guard' EXIT` + INT/TERM. Все `$` экранированы (`__rc=\$?`, `\${__DONE:-0}`, `"\$__rc"`).

Число вставленных `__DONE=1`: **22 присваивания** (20 выходов родителя, включая финальный `exit 0`, + 2 в heredoc t4: таймаут `exit 2` и штатный конец после `echo CONTINUED`).
`grep -c '__DONE=1'` даёт 23: плюс одна строка-комментарий образца. Гейт форм комментарии срезает.

`__DONE=0`: 2 (родитель + t4).

В гвардах `exit 2` / `exit "$__rc"` без `__DONE=1` — это сам часовой, не штатный конец.

Форма `__DONE=1; exit $?` нигде не стоит: код в гварде снимается ` __rc=$? ` / `__rc=\$?` ДО объявления.

## 3. ПОСЛЕ правки

sha256 после починки (и после снятия проб — совпал):
```
9220dac07af602cc9274fcfccf647b789ea69c607c800c548cdccb0e82717e7a  /Users/maratkarimov/work/SIB/Transmutation/Nexus/Catalyst/Catalyst-CC-Patch/tools/gate-kill-teeth.sh
```

`bash -n` родителя: `N_RC=0`.

Гейт форм ПОСЛЕ (дословно; A/B уже зелёные, поэтому весь прогон rc=0, не 1):
```
ФОРМЫ ОБОЛОЧКИ ЧИСТЫ: разобрано файлов 21
GATE_AFTER_RC=0
```
Нашего файла в выводе нет.

Зубы ПОСЛЕ (дословная итоговая строка; `TEETH_AFTER_RC=0`):
```
gate-kill-teeth: teeth=4 green=4 mutations=4 held=4
```
Счётчик не уменьшился, новых пропусков нет.

## 4. §5.4 — часовой внутри сгенерированного t4.sh (живой)

Временная печать: после `write_t4` копирование в
`~/work/SIB/Transmutation/Nexus/Catalyst/programs/2026-09-16-shell-sentinel-165/t4-generated.sh`,
затем `__DONE=1; exit 0`. Прогон: `DUMP_RC=0`, строка `DUMPED t4.sh to program home`.
Печать снята, sha256 вернулся к `9220dac0…`.

`bash -n` сгенерированного: `T4N_RC=0`.

Фрагмент сгенерированного файла (значения НЕ подставлены родителем):
```
__DONE=0
__t4_guard() {
  __rc=$?
  cleanup_t4
  if [[ "${__DONE:-0}" != 1 && "$__rc" == 0 ]]; then
    echo "ОТКАЗ: t4 оборвался, не дойдя до конца (ошибка оболочки выше)" >&2
    exit 2
  fi
  exit "$__rc"
}
trap '__t4_guard' EXIT
trap 'exit 130' INT
trap 'exit 143' TERM
```

Контроль: `__rc=$?` (не пусто и не код родителя), `${__DONE:-0}` живой, `"$__rc"` живой.
Полный снимок — `t4-generated.sh` в доме волны. HELPERS/WORKDIR в снимке указывают на уже снятый mktemp прогона-дампа; это улика генерации, не для повторного запуска.

## 5. Красный контроль (замена §5.3): `exit 0` без `__DONE=1`

Временно снята строка `__DONE=1` перед штатным `exit 0` (единственный нулевой выход файла).
Прогон дошёл до `teeth=4 green=4 mutations=4 held=4`, затем:
```
ОТКАЗ: gate-kill-teeth оборвался, не дойдя до конца (ошибка оболочки выше)
RED_CONTROL_RC=2
```
Строка возвращена. sha256 до пробы = после пробы:
```
9220dac07af602cc9274fcfccf647b789ea69c607c800c548cdccb0e82717e7a
9220dac07af602cc9274fcfccf647b789ea69c607c800c548cdccb0e82717e7a
```

## 6. Пины

Пинов в файле на текст трапа нет. Команда проверки: `grep -l 'gate-kill-teeth' tools/*.sh claude-patch-all.sh` — только сам файл. Блок `mut.1`–`mut.4` old/new не тронут (хелперы кита, не часовой).

## 7. Concerns

- Посылка правила 3 (обрыв на фатальной подстановке → rc=0) опровергнута замером `MEASURE-trap-masking.md`. Часовой всё же нужен: сигналы 130/143, ветки `exit 0` без объявления, и гейт форм. Ловушка §2.2 (`__DONE=1; exit $?` = код присваивания) жива; в этой правке код снимается до `__DONE=1`.
- Пре-существующий шум cleanup: `kill: (pid) - No such process` на уже снятых pid из `${PIDS}`. Был ДО правки, в бриф не входит, не трогал.
- `git status` показывает `tools/gate-kill-teeth.sh` как `??` (файл был не в индексе до этой дорожки, mtime 12:41). Не коммитил.

## Self-Check: PASSED

- Целевой файл существует, отчёт существует, снимок t4-generated.sh существует.
- Счётчики — дословный вывод реально запущенных команд (гейты, зубы, sha256, RED_CONTROL_RC=2, DUMP_RC=0, N_RC=0, T4N_RC=0).
- Строка статуса DONE соответствует телу: гейт форм без нашего файла, зубы 4/4 до и после, t4 живой, красный контроль rc=2, sha совпал.
- A/B файлы и отчёты не писались.
- Коммита нет.
