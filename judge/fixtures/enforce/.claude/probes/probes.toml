# Project layer of the fixture: a cancellation that reached the model. enforce makes
# the verdict take effect, and the project rule in judge/prompt.extra.md gives the reason to cancel.
[probe.judge]
timeout_ms = 60000
max_tokens = 3000
enforce = true

[[probe.judge.models]]
model = "deepseek-v4-flash"
effort = "high"
