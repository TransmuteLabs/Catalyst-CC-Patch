# Дом пола поддержки.
#
# Пол -- свойство НАШЕЙ поддержки (какие версии мы обещаем патчить), а не
# свойство того, что лежит на диске конкретной машины. Пока пол был
# производной от списка корпуса, два платформенных списка означали бы ДВА
# ПОЛА и молчаливое расхождение: одна и та же версия оказалась бы
# поддерживаемой на одной платформе и снесённой прополкой на другой.
#
# Одна активная строка, поля через табуляцию: версия, дата подъёма, основание.
# Пустой файл, две активные строки, версия не вида N.N.N -- отказ разборщика
# (tools/corpus-list.py --floor), не молчаливый дефолт.
#
# История подъёмов -- здесь, рядом с действующим полом: объявление
# «последние пять» само по себе не говорит, какой пол действовал в день
# замера, а замеры в дереве датированы.
#   13.09, первое:  последние ПЯТЬ выпусков, но НЕ НИЖЕ 2.1.267.
#   13.09, второе:  пол поднят до 2.1.270 -- активная версия одна.
#   15.09, третье:  пол поднят до 2.1.272 («272 становится минимальной
#                   поддерживаемой»); 2.1.271 перешагнут -- он не был ни
#                   замерен, ни запинен.
#   18.09, четвёртое: правило вместо числа -- «минимальная версия claude
#                   теперь всегда последняя (если меньшая версия не нужна
#                   для сравния идругого типа задач)». Число в активной
#                   строке с этого дня следует за ГОЛОВОЙ, и 273/274/275
#                   перешагнуты сознательно: сравнительные задачи берут их
#                   пинами, а не полом.
#   18.09, пятое:   «мы работает всегда с последней версией» -- область шире
#                   бинарника: мод и кит держатся на голове тем же правилом.
#                   Голова дня -- 2.1.276.
#   20.09, шестое:   слово юзера: «работаем только с 278» -- голова дня
#                   2.1.278; 277 перешагнута: прогона конвейера по ней не было.
2.1.278	2026-09-20	слово юзера: работаем только с 278
