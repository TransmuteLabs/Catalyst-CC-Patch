# Дом реестра ВЫКЛЮЧЕННЫХ шагов нашего патча (tweakcc-patch.js).
#
# Назначение: шаг остаётся в файле патча КОДОМ, но НЕ исполняется. Слово юзера
# (2026-09-19): «код остается как логика на случай необходимости но не
# исправляется когда сломан пока не нужен. тоесть логика патча просто отключена.
# лежит в файле патча но не отрабатывает». Выключенный шаг не краснит прогон,
# даже если его локатор протух: он не бежит, ловить нечего. Обратное включение --
# удаление строки здесь, без возврата кода.
#
# Формат: три поля через TAB:
#   <имя шага ровно как в step('…')>	<версия-пол>	<причина выключения одной фразой>
#
# Версия-пол -- первая версия, ОБРАЗЫ которой собираются уже без этого шага.
# КОНСТРЕЙНТ: версия-пол отвечает на вопрос «мог ли ЭТОТ образ нести правило
# по праву» (образ старше пола -- правило в нём по праву) и к стороне
# компоновки отношения не имеет: слово юзера выключает шаг безусловно, на
# любой сборке, поле читает только проверяющая сторона.
#
# Разбор (два читателя: компоновка в claude-patch-all.sh перед стадией 3 и
# верификатор в том же файле): пустая строка и строка-комментарий пропускаются;
# неразобранная строка -- ОТКАЗ с её номером; две строки на одно имя -- ОТКАЗ
# (читатель взял бы первую, а человек правил вторую). ОТСУТСТВУЮЩИЙ или ПУСТОЙ
# файл -- НОРМА: все шаги включены, отказываться не о чем.
#
# Имя-призрак (нет ни одного step('…') с таким именем) -- ОТКАЗ ПРИБОРА в самом
# раннере: опечатка обязана быть слышной, иначе «выключили» молча не выключит
# ничего. Раннер получает реестр ТЕКСТОМ скрипта (песочница adhoc не имеет ни
# файловой системы, ни окружения) -- claude-patch-all.sh подставляет записи в
# якорную строку STEPS_OFF раннера перед вызовом tweakcc.
#
# Каждая новая выключенная запись обязана В ТОЙ ЖЕ ВОЛНЕ провести шаг в
# проверяющую сторону (claude-patch-all.sh): у каждого шага свои проверки, и
# без их учёта выключение даст ложную красноту верификатора.
#
# Запись, обоснованная словами «правило несёт мод», верна УСЛОВНО -- только в
# конфигурации, где мод и есть носитель. Верификатор проверяет это условие по
# живому дому (env-карта settings.json: носитель мод -- это function-hooks
# включены, CLAUDE_JUDGE_CARRIER=mod, CLAUDE_JUDGE включён, слой промтов не
# выключен). Носитель не работает или не закреплён -- ПРОГОН КРАСНЕЕТ своим
# отказом «carrier»: правило не несёт никто, и молчать об этом запрещено.
#
# Текст врезки остаётся в файле патча целиком, поэтому сторож побайтового
# паритета (catalyst-probes: COACHING_SPLICE_SHA256, ступень
# tests/scripts/check-splice-parity.sh) продолжает стеречь ОБА дома текста.
26 dispatch-cancellation rule in the system prompt	2.1.278	правило про отменённый диспатч несёт мод catalyst-probes (register.ts, id dispatch-rule); по слову юзера патч-врезка отключена: код шага остаётся в файле, но не исполняется
