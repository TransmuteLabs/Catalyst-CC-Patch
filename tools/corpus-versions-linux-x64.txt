# Корпус пристинных образов для свипа на linux x64: метка и версия.
#
# Список ЭТОЙ платформы. darwin-arm64 живёт в tools/corpus-versions.txt
# (прежнее имя: его пинит гейт свипа). Оба читают один дом формата
# (tools/corpus-list.py). Пол поддержки -- НЕ здесь: он в
# tools/support-floor.txt, один на все платформы, иначе два списка дали бы
# два пола.
#
# Третье поле -- sha256 образа, каким его отдаёт реестр. Пин ставится один
# раз и ТОЛЬКО по байтам реестра. Слово «-» вместо цифр означает «ещё не
# записан»: версию, чей образ не скачан и не запинен, строка объявляет, а
# не пропускает молча.
#
# Активные строки -- каденция на linux (не ниже пола 2.1.272). Пристинного
# linux-272 на замер 17.09 нет; linux-273 на замер 17.09 есть как живой
# образ, байты реестра в этот список не записывались. Пины обеих -- «-».
# platform: @anthropic-ai/claude-code-linux-x64
272 2.1.272 -
273 2.1.273 -
