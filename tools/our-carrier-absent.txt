# Дом объявления отсутствия носителя судьи на площадке (#389).
#
# Назначение: отсутствие носителя -- свойство ПЛОЩАДКИ, а не записи о
# выключенном шаге, поэтому дом отдельный, а не поле our-steps-off.txt:
# вторая запись с тем же основанием иначе продублировала бы перечень
# площадок и разошлась бы с этим домом.
#
# Формат: одна строка на пару «площадка × ручка», три поля через TAB:
#   <площадка>	<имя ручки>	<основание>
#
# Площадка -- имя хоста до первой точки (uname -n без mDNS-суффикса
# .local): имя принадлежит МАШИНЕ, а не ядру (uname -s не годится -- обе
# площадки могут оказаться Linux). Ключ поручечный, а не площадочный:
# появится на площадке CLAUDE_JUDGE, но не CLAUDE_JUDGE_CARRIER -- отказ
# carrier обязан вернуться; общее объявление «носителя тут нет» это скрыло бы.
#
# CONSTRAINT (протухание): запись о площадке, которой больше нет, изнутри
# одной машины не детектируется -- прибор видит только свой хост.
# Направление fail-safe: сменилось имя хоста -- запись не подходит, отказ
# carrier возвращается, то есть в сторону громкости.
#
# Разбор (проверяющая сторона в claude-patch-all.sh): пустая строка и
# строка-комментарий пропускаются; неразобранная строка -- ОТКАЗ с её
# номером; две строки на одну пару «площадка × ручка» -- ОТКАЗ (читатель
# взял бы первую, а человек правил вторую). ОТСУТСТВУЮЩИЙ или ПУСТОЙ файл --
# НОРМА: ничего не объявлено, отказ carrier при неработающем носителе
# сохраняется.
#
# Для мака (mmm4p) записей НЕТ: там носитель судьи работает.
ds7992137	CLAUDE_JUDGE_CARRIER	#236: носителей моделей на этой площадке нет по построению, сеть до api.anthropic.com мертва в обе стороны, судить некому
ds7992137	CLAUDE_JUDGE	#236: носителей моделей на этой площадке нет по построению, сеть до api.anthropic.com мертва в обе стороны, судить некому
