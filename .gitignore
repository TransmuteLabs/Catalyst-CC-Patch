# записи, журналы и результаты прогонов живут в доме развёртывания,
# а не в каноне: они данные машины, а не исходник проекта
judge/records/
judge/journal.jsonl
judge/bench/
judge/labels.jsonl
__pycache__/
*.pyc

# комплект — производная от живых файлов (scripts/kit-build.sh собирает его
# заново); дом каждого файла на диске, архив в истории только раздувал репозиторий
dist/
