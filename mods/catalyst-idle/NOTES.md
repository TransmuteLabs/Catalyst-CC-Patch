# catalyst-idle

Watcher from insertion 22. Does not cancel. NUDGE delivery is `$.ui.toast`
(string form accepted; notify/nudge/alert are not functions). Fleet silence:
Agent/Task calls skip consultation (the fleet is about to work). `$.agent.list()`
returns an array (empty at session.start). Cooldown from `[probe.idle-watch]`.
